services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: sciot-mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./deploy/mosquitto:/mosquitto/config:ro
      - mosquitto-data:/mosquitto/data
      - mosquitto-log:/mosquitto/log

  # hub:
  #   build: .
  #   container_name: sciot-hub
  #   restart: unless-stopped
  #   depends_on:
  #     - mosquitto
  #   environment:
  #     MQTT_HOST: mosquitto
  #     MQTT_PORT: "1883"
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./data:/app/data

volumes:
  mosquitto-data:
  mosquitto-log:
