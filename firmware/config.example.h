// NODE_ID doubles as the MQTT client id: it must be unique per device.
// Two devices sharing an id kick each other off the broker in a loop.
// Convention: node-plant1, node-plant2, node-greenhouse.
#define NODE_ID ""
#define WIFI_SSID ""
#define WIFI_PASS ""
#define MQTT_HOST ""
